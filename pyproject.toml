[project]
name = "qc_launcher"
version = "0.2.0"
authors = [
    { name = "Peichen Zhong" },
    { name = "Yicheng Chen" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ase",
    "pyyaml",
    "pyscf<=2.10.0",
    "sella",
]
[project.scripts]
qc_launch = "qc_launcher.cli:main"
[project.optional-dependencies]
gpu4pyscf = [
    "cupy-cuda12x==13.4.1",
    "cutensor-cu12==2.2.0",
    "gpu4pyscf-cuda12x",
]
gsm = [
    "pygsm @ git+https://github.com/X1X1010/pygsm.git",
]
pysisyphus = [
    "pysisyphus",
]
mace = [
    "mace-torch"
]
fairchem = [
    "fairchem-core"
]
[tool.setuptools]
packages = { find = { where = ["src"] } }
package-dir = { "" = "src" }
